// import { fromJS } from 'immutable';
// import { makeSelectgithubDomain } from '../selectors';

// const selector = makeSelectgithubDomain();

describe('makeSelectgithubDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
